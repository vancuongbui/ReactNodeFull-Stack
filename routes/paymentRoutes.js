// handling all auth routes
const passport = require('passport');

module.exports = (app) => {
    // app.get('/api/payment', passport.authenticate('google', {
    //     scope: ['profile', 'email'],
    //     // google have profile and email for using.
    // }));
}

